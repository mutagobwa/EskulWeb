@* @model Eskul.Models.HostelRoom
@{

    var editmodel = Model.roomBeds;
}
<div class="modal fade" id="modal-default">
    <div class="modal-dialog">
        <div class="modal-content box box-primary" style="width:350px;">
            <div class="modal-header">
                <h4 class="modal-title">Add Room</h4>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>

            <div class="modal-body">
                <form asp-action="ScoreComment" id="ReturnBook" class="needs-validation" novalidate>
                    <div class="form-group">
                        <input type="hidden" id="RoomId" asp-for="RoomId" />
                        <label asp-for="@Model.roomBeds.BedName" style="font-weight: normal !important;">Bed Name</label>
                        <input asp-for="@Model.roomBeds.BedName" name="Comment" class="form-control form-control-sm" autocomplete="off"/>
                        <div class="invalid-feedback">This Field is Required</div>
                    </div>
                    <div class="modal-footer justify-content-between">
                        <input type="submit" value="Save" class="btn btn-block btn-sm mybtn" />
                    </div>
                </form>
            </div>
        </div>
        <!-- /.modal-content -->
    </div>
    <!-- /.modal-dialog -->
</div> *@